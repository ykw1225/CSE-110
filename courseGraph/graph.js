
// call getCourseMap function when a user clicks on a course

// variable which represents the course element that user click on
var courseSelected = document.getElementById("courseSelected");

// variable for temporary store the courses return by getCourseMap
var courseNeeded = [];

// listener of click action
courseSelected.addEventListener("click", callGetCourseMap());

// function that call getCourseMap and stores courses that returned
function callGetCourseMap(){
	// call getCourseMap function
	// store all the courses needed from the server
}

// make a graph for all these courses